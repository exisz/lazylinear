# Command Reference

> Auto-generated from `linear --help`. Do not edit manually.

## `linear`

```
usage: linear [-h] [--version] [--json]
              {viewer,teams,states,issues,read,create,move,comment,comments,query} ...

Small Linear CLI shim (lazylinear)

positional arguments:
  {viewer,teams,states,issues,read,create,move,comment,comments,query}
    viewer              show authenticated user/org
    teams               list teams
    states              list workflow states for a team
    issues              list issues
    read                read issue by identifier
    create              create issue
    move                move issue to state by name
    comment             add comment to issue
    comments            list comments for an issue
    query               run raw GraphQL

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit
  --json                output JSON
```

### `linear viewer`

```
usage: linear viewer [-h]

options:
  -h, --help  show this help message and exit
```

### `linear teams`

```
usage: linear teams [-h] [--limit LIMIT]

options:
  -h, --help     show this help message and exit
  --limit LIMIT
```

### `linear states`

```
usage: linear states [-h] team

positional arguments:
  team

options:
  -h, --help  show this help message and exit
```

### `linear issues`

```
usage: linear issues [-h] [--team TEAM] [--limit LIMIT]

options:
  -h, --help     show this help message and exit
  --team TEAM    team key, e.g. EDU
  --limit LIMIT
```

### `linear read`

```
usage: linear read [-h] issue

positional arguments:
  issue

options:
  -h, --help  show this help message and exit
```

### `linear create`

```
usage: linear create [-h] --team TEAM [--description DESCRIPTION]
                     [--state STATE] [--label LABEL]
                     title

positional arguments:
  title

options:
  -h, --help            show this help message and exit
  --team TEAM
  --description DESCRIPTION
  --state STATE
  --label LABEL
```

### `linear move`

```
usage: linear move [-h] issue state

positional arguments:
  issue
  state

options:
  -h, --help  show this help message and exit
```

### `linear comment`

```
usage: linear comment [-h] issue body

positional arguments:
  issue
  body

options:
  -h, --help  show this help message and exit
```

### `linear comments`

```
usage: linear comments [-h] [--limit LIMIT] issue

positional arguments:
  issue

options:
  -h, --help     show this help message and exit
  --limit LIMIT
```

### `linear query`

```
usage: linear query [-h] [--variables VARIABLES] graphql

positional arguments:
  graphql

options:
  -h, --help            show this help message and exit
  --variables VARIABLES
                        JSON variables
```

---

*Generated by `scripts/generate_docs.py`*
